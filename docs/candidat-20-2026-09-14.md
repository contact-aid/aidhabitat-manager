# Candidat 1.0.0+20 - 14 septembre 2026

## Integration locale

- aecbe8c : dossiers non affectes reserves aux administrateurs, sans attribution automatique a Coralie.
- 1f48220 et 6f30d48 : publication explicite, provenance du web et correction du repertoire de controle.
- b515085 : confirmation NocoDB des ecritures bibliotheque et identite durable des creations Flutter.

Validation combinee avant changement du numero : 733 tests Flutter, 178 tests
serveur, 43 tests de publication et 20 controles critiques reussis. Flutter
analyze, TypeScript et verification syntaxique serveur sans erreur.

Le numero 20 succede aux paquets locaux 18 et 19. Sa disponibilite dans App
Store Connect doit encore etre verifiee avant tout upload. Aucun upload ni
deploiement n'est autorise par le simple fait de preparer ce candidat.

## Environnement verifie en lecture seule

- API live/ready : 7b47dca77a780780661ccc56c1b0f018d60cd0d1.
- Service API : apps_aidhabitat-api-staging, image figee sha-7b47dca,
  digest sha256:b20370ad4f3b205f187d9f68eea4839362250c5cf4dccf22e68eed2a3ce4793d.
- Service web : apps_aidhabitat-web-staging, image aidhabitat-web:latest.
- Le secret Actions EASYPANEL_API_WEBHOOK est absent. Le secret web n'est pas
  une alternative a ce secret API.
- Les sept controles publics du live stack passent.

Le nom staging ne prouve aucune isolation. Un webhook ne modifie pas a lui
seul le tag fige suivi par l'API : verifier et preparer explicitement la cible
avant deploiement, puis exiger le SHA attendu sur live ET ready.

## Ordre de livraison et recette

1. Produire les artefacts depuis un commit propre, conserver manifests,
   empreintes et symboles hors du repertoire de build temporaire.
2. Tester sur un environnement isole avec donnees synthetiques : creation,
   modification et suppression bibliotheque ; coupure reseau et rejeu ;
   modification pendant envoi ; absence de doublon apres reponse perdue.
3. Verifier les comptes administrateur, Coralie et Christelle : les dossiers
   non affectes ne sont accessibles que par l'administrateur.
4. Recetter la mise a jour web avec une operation offline preexistante, sans
   effacer IndexedDB ni localStorage. Verifier la reprise apres reconnexion.
5. Recetter sur iPad : rotation, miniature, sauvegarde, pastille de sync,
   nom du fichier partage et generation PDF.
6. Deployer et verifier l'API avant de diffuser le nouveau client. L'ancienne
   API ignore clientMutationId et ne fournit pas la nouvelle idempotence.
7. Verifier build, commit et empreinte du web servi ; verifier le numero du
   paquet iPad dans TestFlight. Un artefact local n'est pas une publication.

## Limites conservees

- Pas de migration NocoDB ni d'activation de AIDHABITAT_CONDITIONAL_SYNC.
- Les creations strictement simultanees necessitent encore une contrainte
  d'unicite NocoDB ; les anciens clients sans identifiant stable n'ont pas la
  garantie de rejeu du nouveau client.
- La recette manuelle navigateur/iPad reste distincte des tests automatises.
- Airtable reste strictement en lecture seule. Aucun import de donnees ne fait
  partie de cette livraison.
